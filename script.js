function toCase(text) {
  // write your code here
	let count = text.toLowerCase();
	let count1 = text.toUpperCae();
	return count + "-" + count1;
}

// DO not change the code below

// const text = prompt("Enter text:");
// alert(toCase(text));
